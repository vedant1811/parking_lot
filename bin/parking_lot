#!/usr/bin/env ruby

# Add script to run program here.
# Complete bin/setup so that after it is
# run, bin/parking_lot can be used to launch
# it.

# load 'app/controller.rb'
project_bin_dir = File.join(File.dirname(File.expand_path(__FILE__)))
load File.join(project_bin_dir, '..', 'functional_spec', 'app', 'main.rb')
